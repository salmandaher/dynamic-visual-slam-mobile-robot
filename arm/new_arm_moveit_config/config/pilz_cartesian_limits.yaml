# Cartesian limits for the Pilz industrial motion planner (PTP/LIN/CIRC).
# Only used if you select the 'pilz_industrial_motion_planner' pipeline; the
# default OMPL pipeline ignores these.
cartesian_limits:
  max_trans_vel: 1.0
  max_trans_acc: 2.25
  max_trans_dec: -5.0
  max_rot_vel: 1.57
